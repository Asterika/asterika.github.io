//create onboard
$(() => {

//=========================
//        SET GRID
//=========================

const generateGrid = () => {
  for (let i = 0; i < 16; i++) {
    const $button = $('<button>').attr('id', 'square').appendTo('#container');
  }
}

generateGrid();

})
